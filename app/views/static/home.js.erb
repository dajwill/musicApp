$("#music").html("<%= j render("music") %>");
history.pushState(null, '', '<%= url_for(params) %>')
activateGallery()
activateSearchTab()
activatePlayer()
activateCardMenu()
activateSongActions()
