$("#music").html("<%= j render("music") %>");
history.pushState(null, '', '<%= url_for(params) %>')
activateShowActions()
activateGallery()
activateSearchTab()
activatePlayer()
activateCardMenu()
