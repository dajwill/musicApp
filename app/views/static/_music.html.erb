<% if @search.present? %>
  <div class="ui tabular menu">
    <div class="item" data-tab="artists">Artists</div>
    <div class="item" data-tab="albums">Albums</div>
    <div class="item" data-tab="songs">Songs</div>
  </div>
  <div class="ui tab" data-tab="artists">
    <h1>Artists</h1>
    <div class="ui cards">
      <% @search["artist"].each do |artist| %>
        <div class="card">
          <!-- Artist image should go here -->
          <div class="content">
            <a class="header"><%= artist.name %></a>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  <div class="ui tab" data-tab="albums">
    <h1>Albums</h1>
    <div class="ui cards">
      <% @search["album"].each do |album| %>
        <div class="card">
          <a class="image">
            <img src="<%= album.images.first["url"] %>">
          </a>
          <div class="content">
            <a class="header"><%= album.name %></a>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  <div class="ui tab" data-tab="songs">
    <h1>Songs</h1>
    <% @search["tracks"].each do |song| %>
      <div class="ui list">
        <div class="item">
          <img class="ui avatar image" src="<%= song.album.images.first["url"] %>">
          <div class="content">
            <a class="header"><%= song.name %></a>
            <div class="description"><%= song.album.name %></div>
          </div>
        </div>
      </div>
      <%-# <%= link_to "Play", queue_path, :remote => true %>
    <% end %>
  </div>
  <p><%= @search["tracks"].first.uri%></p>
  <iframe src="https://embed.spotify.com/?uri=<%= @search["tracks"].first.uri%>" width="300" height="380" frameborder="0" allowtransparency="true"></iframe>
<% elsif @search.is_a? Hash %>
  <p>No results found.</p>
<% end %>
